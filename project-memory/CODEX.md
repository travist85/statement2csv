# Codex instructions

Read all files in /project-memory before implementing changes.

Constraints:
- Vercel + Cloudflare R2
- Privacy-first: delete uploads after parsing
- Text-first parsing + OCR fallback
- MVP: PDF -> transactions -> CSV preview/download
